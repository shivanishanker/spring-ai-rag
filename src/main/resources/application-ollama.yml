ai:
  provider: ${AI_PROVIDER:ollama}
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://host.docker.internal:11434}
    chat-model: ${OLLAMA_CHAT_MODEL:mistral}
    embedding-model: ${OLLAMA_EMBEDDING_MODEL:nomic-embed-text}
